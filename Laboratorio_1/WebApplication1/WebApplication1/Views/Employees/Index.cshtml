@model IEnumerable<Employee>

    
@{
    ViewData["Title"]="Index";
}   
    
@if (TempData["ResultOk"] != null)
{
    <hl class="alert-success">@TempData["Resultok"]</hl>
}
<div class="container box-shadow p-5">
    <h1 class="text-center mb-3">Operaciones CRUD usando.Net Core6yEntity Framework Core</h1>

        <div class="col mb-3">
             <a aps-controller = "Employees" asp-action= "Create" class="btn btn-lg btn-primary">
                 <i class="bi bi-file-plus-fill">
                     ¡Adicionar empleado!
                 </i>
             </a>
        </div>
</div>